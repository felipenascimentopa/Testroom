import { RespostaQuestaoModel } from './resposta-questao.model';

describe('RespostaQuestaoModel', () => {
  it('should create an instance', () => {
    expect(new RespostaQuestaoModel()).toBeTruthy();
  });
});
