import { RespostaAtividadeModel } from './resposta-atividade.model';

describe('RespostaAtividadeModel', () => {
  it('should create an instance', () => {
    expect(new RespostaAtividadeModel()).toBeTruthy();
  });
});
