# ============================================================
# BANCO DE DADOS (Neon / PostgreSQL)
# ============================================================
spring.datasource.url=jdbc:postgresql://jdbc:postgresql://ep-patient-cake-au9fn5ne-pooler.c-10.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_iP9bzgXtSfF7
spring.datasource.driver-class-name=org.postgresql.Driver

# ============================================================
# JPA / HIBERNATE
# ============================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ============================================================
# E-MAIL
# ============================================================
# Modo "console": o código aparece no log do backend (bom pra dev)
# Modo "smtp": envia e-mail de verdade (precisa configurar abaixo)
app.email.modo=console
app.email.codigo-expiracao-minutos=15
app.email.remetente=no-reply@testroom.com

# --- Configuração SMTP (só usada se app.email.modo=smtp) ---
# Descomente e preencha quando quiser enviar e-mail de verdade
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=seuemail@gmail.com
# spring.mail.password=xxxx xxxx xxxx xxxx
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
# spring.mail.properties.mail.smtp.starttls.required=true

# ============================================================
# SERVIDOR
# ============================================================
server.port=8080